# 🔊 حل مشاكل الصوت - دليل استكشاف الأخطاء

## ⚠️ المشكلة: لا أسمع صوت عند الضغط على أيقونة السماعة

هذا دليل شامل لحل جميع مشاكل الصوت في اختبار القراءة الصوتي.

---

## ✅ الحلول السريعة (جرّب هذه أولاً)

### 1️⃣ تحقق من المتصفح
**الحل:**
- ✅ استخدم **Google Chrome** (الأفضل)
- ✅ أو استخدم **Microsoft Edge**
- ❌ **Firefox و Safari** لهما دعم محدود

### 2️⃣ تحقق من مستوى الصوت
**الحل:**
- ارفع صوت جهازك إلى 70% على الأقل
- تحقق من أن الصوت غير مكتوم
- تحقق من صوت المتصفح (قد يكون مكتوماً بشكل منفصل)

### 3️⃣ انتظر قليلاً
**الحل:**
- عند فتح الصفحة لأول مرة، انتظر 3-5 ثوانٍ
- أحياناً تحتاج الأصوات وقتاً للتحميل
- اضغط على أيقونة السماعة مرتين إذا لم يعمل من المرة الأولى

### 4️⃣ أعد تحميل الصفحة
**الحل:**
- اضغط `Ctrl + R` (Windows) أو `Cmd + R` (Mac)
- أو اضغط زر التحديث في المتصفح
- جرّب الصوت مرة أخرى

---

## 🔧 حلول متقدمة

### المشكلة 1: الصوت يعمل أول مرة فقط

**السبب:** 
المتصفح يحتاج لتفاعل المستخدم أولاً لتشغيل الصوت.

**الحل:**
هذه المشكلة تم حلها في الإصدار الجديد! الملف المحدث يتضمن:
```javascript
// إيقاف أي نطق سابق قبل البدء بنطق جديد
window.speechSynthesis.cancel();
```

### المشكلة 2: الصوت يعمل على الكمبيوتر ولا يعمل على الموبايل

**السبب:**
بعض الهواتف لها إعدادات مختلفة للصوت.

**الحل:**
- تأكد من أن الهاتف ليس في الوضع الصامت
- ارفع مستوى صوت الوسائط (Media Volume)
- جرّب على Chrome Mobile (ليس Safari على iPhone)

### المشكلة 3: الصوت بطيء جداً أو سريع جداً

**السبب:**
إعدادات السرعة في الكود.

**الحل - تعديل السرعة:**
افتح `index.html` وابحث عن:
```javascript
utterance.rate = 0.8; // سرعة النطق
```

غيّر القيمة:
- `0.5` = بطيء جداً
- `0.8` = بطيء (افتراضي)
- `1.0` = عادي
- `1.5` = سريع
- `2.0` = سريع جداً

### المشكلة 4: الصوت منخفض جداً

**السبب:**
مستوى الصوت في الكود منخفض.

**الحل - رفع مستوى الصوت:**
افتح `index.html` وابحث عن:
```javascript
utterance.volume = 1; // مستوى الصوت
```

القيمة يجب أن تكون `1` (أعلى صوت). إذا كانت أقل، غيّرها إلى `1`.

### المشكلة 5: الصوت يعمل للإنجليزية فقط ولا يعمل للعربية

**السبب:**
اللغة غير محددة بشكل صحيح.

**الحل:**
الكود المحدث يحدد اللغة تلقائياً:
```javascript
if (/[\u0600-\u06FF]/.test(currentWord)) {
    utterance.lang = 'ar-SA'; // عربي
} else if (/[\u0590-\u05FF]/.test(currentWord)) {
    utterance.lang = 'he-IL'; // عبري
}
```

### المشكلة 6: رسالة خطأ "متصفحك لا يدعم النطق الصوتي"

**السبب:**
المتصفح قديم أو لا يدعم Web Speech API.

**الحل:**
1. حدّث متصفحك لآخر إصدار
2. استخدم Chrome أو Edge
3. تحقق من دعم المتصفح على [Can I Use](https://caniuse.com/speech-synthesis)

---

## 🧪 اختبار الصوت

### اختبار بسيط:
افتح Console في المتصفح (F12) واكتب:
```javascript
const test = new SpeechSynthesisUtterance('مرحبا');
test.lang = 'ar-SA';
window.speechSynthesis.speak(test);
```

**النتائج:**
- ✅ **إذا سمعت "مرحبا"**: الصوت يعمل! المشكلة في الكود
- ❌ **إذا لم تسمع شيئاً**: المشكلة في المتصفح أو الجهاز

---

## 🌐 دعم المتصفحات

| المتصفح | Windows | Mac | Android | iOS |
|---------|---------|-----|---------|-----|
| Chrome | ✅ ممتاز | ✅ ممتاز | ✅ جيد | ⚠️ محدود |
| Edge | ✅ ممتاز | ✅ ممتاز | ✅ جيد | ❌ غير مدعوم |
| Firefox | ⚠️ جيد | ⚠️ جيد | ⚠️ محدود | ⚠️ محدود |
| Safari | ⚠️ محدود | ⚠️ محدود | ❌ غير مدعوم | ⚠️ محدود |

**التوصية:**
استخدم **Chrome** أو **Edge** على سطح المكتب للحصول على أفضل تجربة!

---

## 🔊 الأصوات المتاحة

### لمعرفة الأصوات المتاحة على جهازك:
افتح Console (F12) واكتب:
```javascript
speechSynthesis.getVoices().forEach(voice => {
    console.log(voice.name, voice.lang);
});
```

### لاستخدام صوت محدد:
في ملف `index.html`، أضف:
```javascript
const speakWord = () => {
    window.speechSynthesis.cancel();
    
    const utterance = new SpeechSynthesisUtterance(currentWord);
    utterance.lang = 'ar-SA';
    
    // اختيار صوت محدد
    const voices = window.speechSynthesis.getVoices();
    const arabicVoice = voices.find(voice => voice.lang.includes('ar'));
    if (arabicVoice) {
        utterance.voice = arabicVoice;
    }
    
    window.speechSynthesis.speak(utterance);
};
```

---

## 📱 مشاكل الموبايل الشائعة

### iPhone/iPad (Safari)
**المشكلة:** الصوت لا يعمل بشكل جيد على Safari.
**الحل:** 
- استخدم Chrome على iPhone بدلاً من Safari
- أو اطلب من المستخدمين فتح الموقع على كمبيوتر

### Android
**المشكلة:** الصوت متقطع.
**الحل:**
- أغلق التطبيقات الأخرى
- تأكد من اتصال الإنترنت
- حدّث Chrome إلى آخر إصدار

---

## 🔍 التحقق من Console للأخطاء

افتح Console (اضغط F12) وابحث عن:

### خطأ شائع 1:
```
Uncaught (in promise) DOMException: play() failed
```
**الحل:** المستخدم يجب أن يتفاعل مع الصفحة أولاً (ضغط زر مثلاً)

### خطأ شائع 2:
```
SpeechSynthesis: language not supported
```
**الحل:** اللغة غير مدعومة على هذا الجهاز

### خطأ شائع 3:
```
speechSynthesis is not defined
```
**الحل:** المتصفح لا يدعم Web Speech API، استخدم Chrome

---

## ✨ التحسينات في الإصدار الجديد

الملف المحدث يتضمن:

✅ **إيقاف الأصوات السابقة:** `window.speechSynthesis.cancel()`
✅ **معالجة الأخطاء:** رسائل خطأ واضحة
✅ **تحديد اللغة التلقائي:** عربي، عبري، أو إنجليزي
✅ **تسجيل الأحداث:** `console.log` للتتبع
✅ **إعدادات محسّنة:** سرعة، طبقة صوت، ومستوى صوت

---

## 🆘 لا يزال لا يعمل؟

جرّب هذه الخطوات بالترتيب:

1. ✅ أعد تشغيل المتصفح
2. ✅ امسح Cache المتصفح
3. ✅ جرّب على جهاز آخر
4. ✅ تأكد من تحديث ملف `index.html` بالإصدار الجديد
5. ✅ اختبر الصوت على موقع آخر للتأكد من أن المشكلة ليست في جهازك

---

## 📞 الدعم الفني

إذا جربت كل الحلول ولا يزال الصوت لا يعمل:

1. **تحقق من Console** (F12) وابحث عن رسائل الخطأ
2. **اختبر على Chrome** بدلاً من متصفحك الحالي
3. **جرّب على كمبيوتر آخر** للتأكد من أن المشكلة ليست في جهازك

---

## 💡 نصيحة مهمة

**الصوت يعمل بشكل أفضل عندما:**
- ✅ يتم فتح الصفحة عبر HTTPS (GitHub Pages يوفر ذلك)
- ✅ المستخدم يتفاعل مع الصفحة أولاً (ضغط زر)
- ✅ المتصفح محدّث لآخر إصدار
- ✅ اتصال الإنترنت مستقر

---

**نتمنى أن يحل هذا الدليل جميع مشاكل الصوت! 🎉**
