# 📝 مثال: تخصيص قائمة الكلمات

هذا الملف يوضح كيفية تخصيص قائمة الكلمات في الاختبار الصوتي.

## 🔧 كيفية التعديل

1. افتح ملف `index.html`
2. ابحث عن السطر التالي (تقريباً في السطر 60):

```javascript
const [words, setWords] = useState([
    'كتاب', 'قلم', 'مدرسة', 'معلم', 'طالب', 'درس', 'صف', 'سبورة',
    'ספר', 'עט', 'בית ספר', 'מורה', 'תלמיד', 'שיעור', 'כיתה', 'לוח'
]);
```

3. استبدله بقائمة الكلمات الخاصة بك

---

## 📚 أمثلة جاهزة

### مثال 1: كلمات عربية بسيطة (للصف الأول)
```javascript
const [words, setWords] = useState([
    'أب', 'أم', 'بيت', 'باب', 'قط', 'كلب', 'شمس', 'قمر',
    'ماء', 'نار', 'يد', 'رأس', 'عين', 'أذن', 'فم', 'قدم'
]);
```

### مثال 2: كلمات عربية متوسطة (للصف الثاني والثالث)
```javascript
const [words, setWords] = useState([
    'حديقة', 'مكتبة', 'نافذة', 'سيارة', 'طائرة', 'قطار',
    'فراشة', 'عصفور', 'أرنب', 'حصان', 'زرافة', 'فيل',
    'تفاحة', 'موزة', 'برتقالة', 'عنب', 'خوخ', 'كرز'
]);
```

### مثال 3: كلمات عربية متقدمة (للصف الرابع وما فوق)
```javascript
const [words, setWords] = useState([
    'الجمهورية', 'الديمقراطية', 'الاستقلال', 'المسؤولية',
    'الصداقة', 'الأمانة', 'التعاون', 'الاحترام',
    'التضامن', 'العدالة', 'الحرية', 'المساواة'
]);
```

### مثال 4: كلمات عبرية بسيطة
```javascript
const [words, setWords] = useState([
    'אבא', 'אמא', 'בית', 'דלת', 'חתול', 'כלב',
    'שמש', 'ירח', 'מים', 'אש', 'יד', 'ראש'
]);
```

### مثال 5: كلمات إنجليزية
```javascript
const [words, setWords] = useState([
    'cat', 'dog', 'house', 'tree', 'book', 'pen',
    'water', 'fire', 'sun', 'moon', 'star', 'sky'
]);
```

**ملاحظة:** إذا استخدمت كلمات إنجليزية، تحتاج لتعديل لغة التعرف على الصوت أيضاً.

### مثال 6: مزيج من اللغات
```javascript
const [words, setWords] = useState([
    'كتاب', 'book', 'ספר',
    'مدرسة', 'school', 'בית ספר',
    'معلم', 'teacher', 'מורה'
]);
```

### مثال 7: كلمات تعليمية حسب الموضوع

#### موضوع: الحيوانات
```javascript
const [words, setWords] = useState([
    'أسد', 'نمر', 'فيل', 'زرافة', 'قرد', 'دب',
    'ثعلب', 'ذئب', 'حمار', 'جمل', 'غزال', 'أرنب'
]);
```

#### موضوع: الفواكه
```javascript
const [words, setWords] = useState([
    'تفاح', 'موز', 'برتقال', 'عنب', 'فراولة', 'مانجو',
    'خوخ', 'كرز', 'تين', 'رمان', 'بطيخ', 'شمام'
]);
```

#### موضوع: الألوان
```javascript
const [words, setWords] = useState([
    'أحمر', 'أزرق', 'أخضر', 'أصفر', 'برتقالي', 'بنفسجي',
    'وردي', 'بني', 'أسود', 'أبيض', 'رمادي', 'ذهبي'
]);
```

#### موضوع: الأرقام بالعربية
```javascript
const [words, setWords] = useState([
    'واحد', 'اثنان', 'ثلاثة', 'أربعة', 'خمسة', 'ستة',
    'سبعة', 'ثمانية', 'تسعة', 'عشرة', 'عشرون', 'مئة'
]);
```

---

## 🎯 نصائح للاختيار

1. **اختر كلمات مناسبة للمستوى:**
   - للمبتدئين: كلمات قصيرة (2-3 أحرف)
   - للمتوسطين: كلمات متوسطة (4-6 أحرف)
   - للمتقدمين: كلمات طويلة أو معقدة

2. **تنوع الأصوات:**
   - اختر كلمات تحتوي على أصوات مختلفة
   - تجنب الكلمات المتشابهة في النطق

3. **الربط بالمنهج:**
   - استخدم كلمات من الدروس الحالية
   - ربط الكلمات بموضوع معين

4. **عدد الكلمات:**
   - للاختبار السريع: 8-12 كلمة
   - للاختبار المتوسط: 16-24 كلمة
   - للاختبار الطويل: 30+ كلمة

---

## 🔄 تحديث الكلمات بعد النشر

إذا نشرت الموقع على GitHub وتريد تحديث الكلمات:

1. اذهب إلى repository الخاص بك
2. اضغط على ملف `index.html`
3. اضغط على أيقونة القلم ✏️ للتعديل
4. عدّل قائمة الكلمات
5. اضغط **Commit changes**
6. انتظر دقيقة أو دقيقتين حتى يتم التحديث

---

## 📱 اختبار الكلمات

بعد التعديل، تأكد من:
- ✅ نطق الكلمات واضح
- ✅ الكلمات مناسبة للمستوى
- ✅ التعرف على الصوت يعمل بشكل جيد
- ✅ لا توجد أخطاء في الكتابة

---

**نتمنى لك تجربة تعليمية ممتعة! 🎓**
